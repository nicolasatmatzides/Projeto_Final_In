<p id="notice"><%= notice %></p>

<div class="col-xs-12 col-sm-12 col-md-12 geral_article">

<div class="col-md-6 personal_article">
    <p><i>
      <strong>User:</strong>
      <%= @article.user_id %>
    </i></p>
    
    <h2>
      <strong>Title:</strong>
      <%= @article.title %>
    </h2>
    
    <p>
      <strong>Content:</strong>
      <%= raw @article.content %>
    </p>
    
    <p><b>
      <strong>Tags:</strong>
      <%= @article.tags %>
    </b></p>

</div>

<h2> Comentários </h2>

<% @comments.each do |comment| %>
    <%= comment.content %>
    <%= comment.user.name %>
    <%= comment.article_id %>
<% end %>
<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
</div>