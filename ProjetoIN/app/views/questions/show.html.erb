<div class="col-sm-12 col-xs-12 col-md-12 questions_show">

  <p id="notice"><%= notice %></p>
  <div class='question-user'>
      <p> <%= link_to @question.user.name, @question.user %></p>
        <%= @question.updated_at %>
  </div>
  <div class='question_title'>

       <h2> <%= @question.title %></h2>
      </p>
  </div>
    <div class='question_content'>

        <%= raw @question.content %>
  </div>
    <div class='question_tags'>

      <p>
        <strong>Tags:</strong>
        <%= @question.tags %>
      </p>
     </div> 
    <!--<div class='btn_criaQuest'> <%= link_to 'Responder', new_answer_path %> </div>-->   
    
    <div class='btnEdit'>
      <%= link_to 'Editar', edit_question_path(@question) %> 
    </div>
    
    
    <!--- RESPOSTAS ------------------------------------------------------>
 <div class='comments'>
  <p> Respostas: </p>
  <% @answers.each do |answer| %>
    <div class='comment-user'>
    <p>
      <%= link_to answer.user.name, answer.user %>
      <p class='comment_date'><%= answer.created_at %></p>
    </p>
  </div>
      <p><%= raw answer.content %></p>
  <% end %>
</div>


<!--- NOVA RESPOSTA -------------------------------------------------->

<div class=novo_comment>Novo Comentário:</div>

<%#, :url => params[:id],  :method => 'post' %>
<%= form_for(@new_answer) do |f| %>
  <% if @new_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_answer.errors.count, "error") %> prohibited this answer from being saved:</h2>

      <ul>
      <% @new_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.cktext_area :content %>
  </div>

  <%= f.hidden_field :question_id, :value => @question.id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

  <div class='botao_voltar'><%= link_to 'Voltar', questions_path %></div>
</div>