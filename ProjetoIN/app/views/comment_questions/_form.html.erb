<%= form_for(comment_question) do |f| %>
  <% if comment_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment_question.errors.count, "error") %> prohibited this comment_question from being saved:</h2>

      <ul>
      <% comment_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %>
    <%= f.number_field :question_id %>
  </div>

  <div class="field">
    <%= f.label :comment_id %>
    <%= f.number_field :comment_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
