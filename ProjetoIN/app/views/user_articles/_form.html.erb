<%= form_for(user_article) do |f| %>
  <% if user_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_article.errors.count, "error") %> prohibited this user_article from being saved:</h2>

      <ul>
      <% user_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :article_id %>
    <%= f.number_field :article_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
